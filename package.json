{
    "name": "arpamail-relay",
    "private": true,
    "version": "0.0.1",
    "scripts": {
        "deploy": "wrangler deploy --strict",
        "dev": "wrangler dev",
        "start": "wrangler dev",
        "cf-typegen": "wrangler types"
    },
    "devDependencies": {
        "typescript": "^6.0.2",
        "wrangler": "^4.79.0"
    }
}
